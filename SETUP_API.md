# 🚀 Запуск API для демонстрации

## Для работы поиска и импорта из дорожной карты нужно запустить два процесса:

### 1️⃣ Запуск мок-API сервера (терминал 1)
```bash
npm run mock-api
```
Сервер запустится на `http://localhost:3001`

### 2️⃣ Запуск React приложения (терминал 2)
```bash
npm run dev
```
Приложение запустится на `http://localhost:5173`

---

## ✅ Что будет работать:

### Поиск технологий
- Введите в поле поиска минимум 2 символа (например: "react", "javascript")
- Результаты будут загружены с мок-сервера
- Каждый результат можно добавить в список

### Импорт из дорожной карты
- В компоненте "Импорт дорожной карты" будет поле с URL API
- По умолчанию стоит: `http://localhost:3001/api/technologies?limit=12`
- Нажмите кнопку "Импорт из API"
- Все 12 технологий будут добавлены в ваш список

---

## 📋 Доступные технологии в API

1. HTML & CSS
2. JavaScript
3. React
4. Node.js
5. Express.js
6. PostgreSQL
7. MongoDB
8. Docker
9. Git
10. REST API
11. GraphQL
12. TypeScript

Каждая технология имеет описание, категорию и рейтинг сложности.

---

## 🔍 Примеры поиска

- `react` - найдет React
- `node` - найдет Node.js
- `data` - найдет базы данных (PostgreSQL, MongoDB)
- `css` - найдет HTML & CSS
- `script` - найдет JavaScript и TypeScript

---

## ⚠️ Если что-то не работает

1. **Убедитесь, что оба сервера запущены** (два разных терминала)
2. **Проверьте, что портов свободны:**
   - `:3001` для мок-API
   - `:5173` для React
3. **Откройте консоль браузера** (F12) - там будут ошибки, если что-то пошло не так

---

Готово! 🎉 Теперь можешь демонстрировать преподавателю!
